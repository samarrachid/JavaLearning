package part3;

/**
 * Update the class:
 * Use the functions Object to use the four basic functions, for first print use the arguments value(the one it define in main method),
 * 	 second time define  a local variable and print the operation
 */

public class FunctionsMain {
	 public static void main (String[] args) {
		 int d = 5;
		 int t = 3;
		 System.out.println("i main sum d before " + d);

		 Functions v = new Functions();
		 v.sum(d, t);
	 }

	 
	 
	 
}
